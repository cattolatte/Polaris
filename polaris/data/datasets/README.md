# Dataset Implementations

This package contains built-in datasets supported by Polaris.

Examples include:

- IMDB
- AG News
- Yelp Reviews

Each dataset implements the common dataset interface defined in `base.py`.